{
  "scripts": {
    "start": "vercel dev",
    "deploy": "vercel deploy --prod",
    "check": "biome check --write --unsafe --diagnostic-level=error",
    "compile": "tsc --noEmit"
  },
  "devDependencies": {
    "@biomejs/biome": "2.0.6",
    "@types/node": "^24.0.8",
    "@vercel/node": "^5.3.0",
    "psn-api": "^2.14.0",
    "typescript": "^5.8.3",
    "vercel": "^44.2.7"
  }
}